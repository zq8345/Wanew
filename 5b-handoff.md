# 下一次开工:第一件该做什么

写于 2026-07-28 收尾。**这不是总结,是接力棒。** 读完照 §1 第一条直接动手。

---

## 1. 5b(/products/ 地址迁移)

### 现在在哪

```
①  ⑨号闸（映射表 ↔ 挂载桩同生同死）      ✅ 已落 main  3588c2028
②  A/B/C 地址来源枚举 + 对账              ✅ 完成（数字见下）
    §3② pages-list 双向闸                 ✅ 已落 main  a3d704cd8
③  regen 停产旧址                         ⛔ 没开工（故意的，见下）
④  内链切换                               ⛔ 没开工
⑤  全量上线 → 491 条 → 给总工信号 → 他 curl
⑥  单独 commit 翻 noindex（唯一不可逆的一下）
```

### 🔴 第一件事:③a —— 把 regen 的四处「存在性判据」从旧址迁到新址

**别先做 ④。** ④ 改起来快、看得见,③ 枯燥 —— 而顺序反了会出一个没人能发现的事故(下面第二段)。

```
scripts/regen.mjs
  249:  if (locale !== DEFAULT && !fs.existsSync(out)) continue;       out = 旧址
  292:  if (!fs.existsSync(pageOf(alt, prod.category/`${id}.html`)))   ← 旧址
  453:  if (!fs.existsSync(p)) continue;                               p   = 旧址 rel
  490:  if (!fs.existsSync(pageOf(mm[1], rel))) return tag;            rel = 旧址
```

**验收 = 产出逐字节不变。** 双活期两套页面都在,锚哪边结果都一样 ⇒ 迁完跑 `node scripts/rebuild.mjs`,**一个字节都不该变**。

> 这个正对照有别的做法没有的性质:**它不需要任何人描述"正确的产出应该长什么样"。**

⚠️ **若不为零,停下来报总工** —— 那说明双活期两套并不等价,是个比"还有第五处"更根本的问题。

⚠️ **不要顺手"把主次调过来"**(现在是旧址为主、新址是复制品)。调主次会改动产出内容,那次「零变化」的正对照就废了。5b 之后旧址那一半直接不产,主次自然只剩一个 —— **为一个会自己消失的问题做重构,是在给自己找一次没有正对照的改动。**

### 🔴 为什么 ③ 必须在 ④ 之前 —— 实测数字,别重新踩

复刻两棵真树,一棵不动,一棵**真删掉那 227 个旧址页**,各跑一次 `regen`:

| | 干净树 | 删掉 227 旧址后 | 差 |
|---|---|---|---|
| `/products/` 详情新址产出 | **190 页** | **68 页** | −122 |
| `/products/{分类}/` 新址产出 | **37 页** | **0 页** | −37 |
| 新址页上的 hreflang alternate | 312 条 | 190 条 | −122 |

**两次 regen 都 rc=0、都完整收尾、零报错。**

> **37 → 0,而闸全绿。** 删旧址不会让 regen 报错,它会让 regen **改变主意** —— 以为 es/pt 这些页「本来就不该有」,于是静默停产。

**先做 ④ 的后果**:内链指向新址,而新址 37 个分类页是陈旧的、122 个 es/pt 页停在几天前 —— **全站 200,没有一个 404 会提醒任何人。**

### ③b 那道闸:**字节级,不是计数级**

我原本提的是「190 / 37 / 312 三个数删前删后必须相等」。**总工把它升级了**:

```
删旧址前后，【所有新址页的产出逐字节相同】
```

计数守不住"页产出来了但内容错了"(alternate 指向错的语种,数量照样 312)。字节相同 ⇒ 数量必然相同。

**两步的判据因此对称,而且都是「零变化」——最不需要解释的一种绿:**
```
③a 迁判据 ：产出逐字节不变    （双活期锚哪边都一样）
③c 删旧址 ：新址页逐字节不变  （删的是旧址，不该影响新址）
```

### ② 的对账结果(5b 的清单,已闭合)

```
磁盘旧址页 240
  ├─ 被 301 映射表覆盖 227   ← 这就是「5b 删 227 页」
  └─ 未覆盖 13：全是 zh，且全是 index.html 列表页
       = 5 个 zh/type/{品类}/ + 8 个 zh/{机型}/，详情页 0 个
       ⇒ 与 DESIGN.md §9.1「zh 不做产品详情页」吻合 ⇒ zh 不迁移不删

映射表 231 条 = 227（对应磁盘）+ 4（zh 孤儿址，文件早已不在，只需 301）
  /zh/industrial/ → /zh/guides/        /zh/mounts/ → /zh/guides/mounts/
  /zh/rv-off-grid/ → /zh/guides/       /zh/power/  → /zh/guides/power/
```

**A/B/C 三类来源(命中 298,分类穷尽):**
- **A 静态字符串 269** —— 其中 **181 是 `data/pages-list.json`(生成物,chrome-sync 从磁盘扫描重写,会自动跟上)**;真人手写的 88 = **11 个模板 × 7**(机型导航,含 `_chrome.html`)= 77 **+** home.html 10 **+** home-featured.json 1
- **B 运行时拼链接 29** —— regen.mjs 9 · render.js 2 · 其余 18 在一次性迁移脚本里(不在产)
- **C 运行时拼写入路径 3** —— 就是上面那四处的所在
  ⚠️ **C 类按构造匹配不到字面量正则**(`pageOf(locale, path.join(prod.category, id+'.html'))` 里没有一个字符是 `/mini/`)。是换了找法(找 `writeFileSync`/`mkdirSync` 站点反推)才找到的。**跨仓那部分(Admin 的写入路径)没扫 —— 因此不敢说它是 0。**

⚠️ **④ 要记的一笔**:首页产品卡的链接是 `/standard/672` —— **旧址**。内链切换必须覆盖首页产品条(它是 `w3-pstrip__card`,和列表页卡片不是同一套组件)。

### ⭐ 方法论:用不变量代替枚举

> **我不敢报「就这 4 处」。真正的判据是:迁完之后产出逐字节不变,才说明没有第五处;变了就是还有。**

今天这条被反复证明是必要的:`normForm` 消费方枚举到 2 个就停了(实际 6 个)· 显示名报 3 份(实际 4 份 + 8 处模板硬编码)· C 类按构造就搜不到。

> **一份必须完整的清单,和一个必须成立的不变量 —— 后者永远更强,因为它不依赖任何人数得全。**

---

## 2. 显示名(与 5b 无关,但会在 5b 收尾碰头)

### 现状:后台改名到不了页面,而且品类和机型病灶不同

**品类(全断)**:Admin 写 `forms.json` 的 `name`;页面标题/chip 走 `regen.mjs:325` 的 `TYPE_TITLE_KEY` → `chrome.json` 的 `header.*`。**两条链平行,谁都没错,但不相交** ⇒ 跑多少次 rebuild 都不变。

**机型(部分断)** —— 用 Joe 的真实数据状态 `e6edfcefb` 复刻实测:

| 页面位置 | 来源 | 改名后跟不跟 |
|---|---|---|
| 列表页 `<title>` / `<h1>` | `locales.json` → `model_display` | ✅ 跟 |
| 详情页面包屑 / Category 标签 | 同上 | ✅ 跟 |
| **nav / footer 链接文字** | `chrome.json` 的 `footer.*` | ❌ |
| **`og:title` / `twitter:title`** | **没有任何东西重写它们** | ❌ 永久不变 |
| JSON-LD `"name"` | 同样没跟上 | ❌ |

🔴 **`og:title` 那条最伤**:`setListTitle`(render.js:614)只 `replace` `<title>`。改名后**同一个页面 `<title>` 说新名、`og:title` 说旧名,而且永久**。og:title 是社媒卡片/聊天预览/AI 抓取读的那份 —— **对外露出旧名,站内显示新名。不是没更新,是自相矛盾。** 优先级高于 nav。

**验收标准(不用再列清单)**:改名后 rebuild,**页面上旧名出现 0 次**。现在实测**还剩 4 处**(nav 1 · og 1 · twitter 1 · JSON-LD 1)。**那个 4 就是清单,归零才算通。**

### 总工要的两问 —— 我的倾向(不是决定,他还没拍)

**问① 直接读品类/机型记录,还是仍生成 `chrome.json`?**

**倾向:直接读。** 因为 `chrome.json` 的键是**从英文显示名 slug 出来的** —— 改名 = 改主键,和昨天从产品身上拆掉的是同一个病。Joe 昨晚已经踩上:把 `Mounts & Brackets` 改成 `Mounts`,而 `header.mounts` **已存在、已被两个模板使用、四语值不同**。

**代价(实测,不是估的)**:
```
模板侧：品类 5 键 = 14 处 · 机型 7 键 = 15 处  合计 29 处
代码侧：regen.mjs:325 TYPE_TITLE_KEY（被 :339 <title> 与 :463 H1/chip 两处消费）
        regen.mjs:164 MODEL = locales.model_display（被 :335/:336 消费）
```
⚠️ 29 处里含 **`_chrome.html`** —— 它被 chrome-sync 烘进每一页,**改它一处 = 动全站**。

**问② 机型真源认 A(`locales.json model_display`)还是 B(并进 `categories.json` 多语言 name)?**

**方向认 B**(Joe 在后台是同一个界面同一个动作,不该有两种真源),**但建议分两步**:
1. 先让读取侧统一到 `model_display`,消灭 `footer.*` 那份 fallback 拷贝 —— 可验证(改名后旧名归零)
2. 再做数据结构合并

理由:`model_display` 有 **9 处引用**,其中 `scripts/i18n-check.mjs` 把它当作**「不翻译的结构常量」白名单**的一部分(`:27`、`:113`)。直接跳到 B 会**同时动数据结构和 i18n 判据**,一批里两件事,又没有正对照。

⚠️ **`chrome.json` 里的 `reason.*` 是机器读的,不是注释**(`catalog-dupe-check.mjs:33` 用 `reason.dupe` 做显式豁免;`brand-residue-scan.mjs:124` 显式跳过 `reason.*`)。搬键的时候 **`brand-residue-scan` 那条跳过规则要跟着搬**,否则品牌残留扫描会假阳性。迁移的验收单位是**整个值对象逐字段相同**,不是"四个语种的字符串相同"。

**问③(总工问的排期)读取侧改造和 ④ 分批还是合批?** —— **合批**,它们碰同一批模板(含 `_chrome.html`)。

⚠️ **另一半,比改名更根本**:模板里那 8 处硬编码 `{{t.header.xxx}}` 是「**哪个品类出现在导航里**」的另一份真源 —— **加一个新品类,导航不会自己长出来**。改名不生效 Joe 试了会发现;**加品类不进导航他可能发现不了**,只会以为"还没构建"。

---

## 3. 未推的东西

**没有。** `origin/main = e6aaf30a0`,本地 `gate9` 与之相同,工作树 0 项。今天推的三条:
```
3588c2028  ⑨ 号闸（映射表 ↔ 挂载桩同生同死）
a3d704cd8  pages-list 双向闸（基线 611≡611，两向都实测能红）
e6aaf30a0  产品卡去摘要（生产 7 页实测，摘要 0）
```

### ⚠️ 三个陷阱,下次开工前先看

**① 本地 `main` 分支是陈旧的** —— `33fefef64`,含 5a,**没跟进重排**。
`git branch --contains` 会因此说"5a 在 main 上",**那是假的**。判真线只看 `git ls-remote origin refs/heads/main`。

**② 5a(`6ef06db6a`)仍压着,不在 origin/main 上。** 它含:301 映射表 `data/product-redirects.json`(231 条)+ 29 个挂载桩 + `legacy-redirect.js` + regen/route-live-check 改动 = 35 个文件。
⚠️ **直接 cherry-pick 会与两个文件冲突,而"取 5a 那边"会静默删掉 ⑩ 号闸**(5a 写于 ⑩ 落 main 之前)。`scripts/i18n-check.mjs` 的冲突正解是**并集**(main 加了 gates/vendor-manifest,5a 加了 product-redirects)。

**③ `main` 现在带两道红,是 Admin 直写带进来的,不是谁改坏的:**
```
es-hold-check  ❌ 4 处（产品 4208-4211 无 es-MX 又没声明扣留；来源 04411c7b1 admin 提交）
pt-leak-scan   真漏译 90（今天从 115 降下来的：卡片去摘要顺带清掉 25 处 pt 页英文泄漏）
```
**Admin 直写 main 完全不过闸** —— Joe 每加一个产品都可能把 main 变红,而没有任何人会知道。

⚠️ **跨仓待办**:`functions/_lib/render.js` 是 `vendor-manifest` 里 `mirror: true` 的文件,今天改了它。**Admin 的漂移守卫会红,且在它重新镜像之前,由 Admin 重新生成的列表页会把卡片摘要放回去。** 已报总工,由他路由给 Admin 窗。

### 闸怎么跑

`data/gates.json` 有 26 道,**没有 runner** —— 每条是 `cmd` 串,自己驱动。
⚠️ **别用管道包住闸**(`cmd | tail` 的 `$?` 是 tail 的)。三段式:`cmd > file 2>&1; rc=$?`。
⚠️ `pages-list-sync` 那道闸的脚本叫 `chrome-sync.mjs` —— **名字里没有 check/verify/scan,glob 永远发现不了它**,只能靠 `gates.json` 里的声明。

---

## 4. 一条今天学到的、值得带进下一次的

**当一个判据给出的结论与你的预期一致时,它同样需要正对照。**

今天我报过一次「Joe 的机型改名到不了页面」,证据是「含旧名的**文件数** 621 → 621,一个字没变」。**那是假阴性** —— 导航来自 `chrome.json`、每一页都有一份,所以只要 chrome.json 不动,**文件数永远不会掉**。我数了元素的容器,没读元素的内容。

⚠️ 前面几个假阴性之所以被抓到,是因为结果"太干净、不合常理";**而这一次结果恰好合理,恰好指向我预期的方向 —— 所以我没回头验材料。**
