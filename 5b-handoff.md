# 下一次开工:第一件该做什么

**这不是总结,是接力棒。** 读完照 §1 第一条直接动手。上一版写于 5b 开工前,已被本版取代。

---

## 1. 顺序(总工写死,别重推)

```
1. ④ 收尾  把【剩下两套组件】接进 productHref —— 🔴 不许写第五条重写规则
2. 【Joe 看得见的三件】导航指真实品类页 · /products/ 不预设筛选 · 筛选状态写进 URL（§1e）
3. ①「第二刀」品类显示名直达页面（§1c，设计已定见 §1c-设计）
4. 常驻闸  旧址 href 即红
5. 合 5a（301 表 231 条）→ 6. ⑤ 报总工 curl → 7. ⑥ 总工做 → 8. ② CI（§1d）
```

> 🔴 **硬规则(总工定,不是建议):④ 收尾遇到任何卡点 —— 又冒出第五套组件、或要动风险大的正则 ——
> 立刻停下,先做第 2 项。** 理由:**看得见的坏优先于看不见的效率。** Joe 已第四次看到同一个问题。

### 🔴 §1e-0 先读这条:那 20 个落地页【现在不值得被落地】(总工查出的真问题)

```
/products/mounts/  h1 "Starlink Mounts" ✅   h1 下方介绍文案：【无】
                   整页可见文字 7903 字符，几乎全是产品卡标题 ⇒ 去掉卡片就什么都不剩
/products/ 15797 字符 —— 与品类页的差别只是"显示哪一批卡"
```
> **5 个品类页彼此只差卡片子集。开放收录后 Google 看到 5 个近乎相同的薄页面** —— 排不上去,
> 还可能被判 doorway。**只把地址理顺 = 骨架对了但没有肉。**

**🔴 验收判据(一句话)**:**把产品卡全部去掉之后,这一页还剩下有价值的内容吗?**
现在是「没有」,做完必须是「有」。

**内容三件(⑤⑥⑦)**:每页 h1 下 2–3 句介绍(这个品类是什么 · 给谁用 · 怎么选,四语)·
每页链到对应 Guide(Mounting / Power / Cabling / Weatherproofing,品类页 ↔ 攻略互链)· 每页独立 meta description。

🔴 **文案硬约束:只写"是什么、怎么选",不许新造任何技术声明** —— 不写防水等级、不写功率数字、
不写兼容性断言。**"选购角度"安全,"参数断言"不是**(另有一批待核产品宣称 #70)。
⚠️ 文案**直接上线,不挂 Joe 审**;他要改,后续后台加编辑框。

**🔴 真源放哪(我的判断,已报总工)**:`intro` 写进**各自实体自己的记录** ——
`forms.json` 每条加 `intro`、`categories.json` 每条加 `intro`。
**不新建 `category-intro.json`** —— 那是"按同一个 slug 再建一张表",就是今天在拆的
`chrome.json header.*` 那个病:两张表靠 slug 关联,迟早一边改了一边没改。
**也不合并成一张共享表** —— 品类和机型是两类实体,合并等于再造一个需要判别类型的字典。

⚠️ **③(筛选写进 URL)总工已自己做完上线 `834edbb0a`** —— `skin/js/w3.js` 加 `syncUrl()`(replaceState)
+ deepLink 认两种输入,浏览器实测往返通过。**下轮开工先 `git pull`。**

### 🔴 §1e Joe 看得见的三件(他自己发现的;③ 已由总工完成)

**① 导航下拉改指真实页面**
```
现在  导航下拉 → /products/#power     客户端筛选，# 对 Google 不存在
      BROWSE 磁贴 → /products/power/  真实页面，有自己的 title/h1/canonical
改：  _chrome.html:15-19 那 5 条 {{url./products/#xxx}} → {{url./products/{cat}/}}，与磁贴统一
```
⚠️ **别删 hash 兼容**:已有的 `#power` 链接/书签落地时仍应自动筛选(`skin/js/w3.js:232` 那段保留)。
🔴 **这才是最值钱的一条**:`/products/{cat}/` 那 20 页(5 品类 × 4 语)真实存在、有标题有 H1,
**却没有任何导航入口、而且挂着 noindex** ⇒ 买家搜 "starlink charging accessories" 时我们一个落地页都没有。

**② `/products/` 保持"全部 68 个"** —— 不带预设筛选;核首次加载(无 hash)时两轴都是 All。

**③ 点筛选把状态写进 URL —— 🔴 用 `history.replaceState`,不要 `pushState`**
```
现在  点 chip → URL 完全不变（w3.js:232 只在【加载时】读一次 hash，之后不写回）
      ⇒ 地址栏显示 /products/ 而画面是 Charging 的 7 个产品 —— **地址栏在说谎**
不用 pushState：连点 5 个 chip 会塞 5 条历史，用户要按 5 次后退才离得开
判据：任意筛选组合下，把地址栏 URL 重新打开，画面必须与操作后一致（含"回到 All"）
```

**④ 那 20 个品类页要进 ⑥ 的 noindex 开放名单** —— ⑥ 原写"新址页关 noindex",
**明确它包含 `/products/{cat}/`,不只详情页**,否则导航指过去、Google 仍然不收。

⚠️ 上线判据同 §1c:`_chrome.html` 一改就烘进 698 页 ⇒ **声明 pairs 逐页核对**,证明差异恰好等于声明的那几处。
🔴 **`chrome-verify` 会因同一原因第二次红 ⇒ 当场把那条判据登记成常驻闸,不用问。一次是判断,两次是流程。**

⚠️ 观察(记账,不在这批):导航 "Shop by Starlink model →" 指 `/products/`,而机型也有真实页(`/products/mini/` 等)。**同一个病的另一半。**

### 🔴 §1c ①「第二刀」—— 品类改名自动同步(Joe 拍板,已是第二次手工补丁)

**触发**:Joe 把 `power` 的显示名改成 `Charging`,`forms.json` 已是 `Charging`,页面全站仍显示 `Power & Charging`。
⚠️ **这次和 Mounts 那次不同,别按上次套路来:**
```
上次  Mounts & Brackets → Mounts     翻译表里【恰好已有】header.mounts，四语齐全 ⇒ 换指向就完事
这次  Power & Charging → Charging    表里【没有】Charging：
        header.power_charging = Power & Charging / Energía y carga / Energia e Carregamento / 电源与充电
        header.power          = Power / Energía / Energia / 电源
⇒ 没有可换的指向。第三次手工补丁不做。
```

**设计已由总工定死,不用来回问:**
```
en        直接读 data/forms.json 的 name（Joe 改什么就是什么）
es/pt/zh  保持 chrome.json 现有值
```
> **西语标签不必是英语的直译** —— 它们是各自语言里对同一品类的叫法。
> 「英文变了、西语没变」**不是 bug**,是"翻译按语言分别维护"。多语言编辑是 Admin 后续加语言 Tab 的活,不在这一刀。

**🔴 验收两条,B 才是重点:**
```
A. forms.json 改 name → rebuild → 四语页面出现新值（en），旧值出现 0 次
B. forms.json 【新增】一个品类 → rebuild → 导航/磁贴/筛选行自动多一项，【不改任何模板】
```
> **A 只证明"能跟着改",B 才证明"不再是硬编码"。** 模板里那 5 个品类现在是一行行写死的 ⇒
> **加一个新品类,导航不会自己长出来,而 Joe 发现不了**(改名不生效他会发现,这个他不会)。

⚠️ 这一刀改 `_chrome.html`,烘进 698 页 ⇒ `chrome-verify` 大概率再红。
**按既定规则:同一道闸同一原因第二次红,当场把机制做出来,不问。** 机制就是那条已验证过的判据 ——
**声明 pairs + 逐页核对 = 证明差异恰好等于声明的那一处**(上次实测 611/611、零夹带),登记成闸。

⚠️ **① 做完先别自己跑生产重建** —— 报总工,**重建由他发起**(动 698 页,他要先确认 ④ 已绿)。

### §1d ② CI 自动构建(⑥ 之后,现在只需知道它要来)

```
data/ 改动 → GitHub Actions 跑 rebuild → 跑 gates.json 27 道闸 → 全绿才提交 built 页面 → Pages 发布
一道红 = 不发布
```
**为什么必须 CI、不能让 Admin 写页面**(总工已跟 Joe 讲过,记此免得重推):
```
698 个 html / 19.8 MB，品类名印在每一页导航里
Admin 一次保存要重写 698 个文件 ⇒ 远超 Worker 每次调用 50 次子请求的硬上限
⇒ 结构上做不到，不是代码问题
```
🔴 **上线前必须先验的一点**:仓 `core.autocrlf=true`,**本地检出 CRLF、Linux runner 产出 LF**。
先在分支上验一次「**CI 产出 vs 本地产出逐字节相同**」—— 与 2026-07-27 记过的那个 EOL 假漂移同族,只是这次两边是**不同的机器**。

### 第一件:④ 收尾 —— **减规则,不是加规则**

**产品链接到今天为止出现在四套长得不一样的组件里:**
```
cardHtml · w3-pstrip__card · product-chip · product-grid-link
前三套的产品链接已在 9a8bc985d 收敛进 productHref(e)
```
**仍在产页面上还剩 283 处旧址 href,分两类:**
```
href="/mini/44" 一类   = 产品详情链接 ⇒ 本来就该走 productHref(e)，【不要写新正则】
product-grid-link 一类 = 机型/品类入口 ⇒ 与 w3-pstrip__card 走同一个出口（没有就抽一个）
```
> 🔴 **每加一条重写规则,就是又一份会各自漂的拷贝。** 三份拷贝 = 三条会各自漂的规则,
> 而漂开的表现是"其中一处还指着旧址",全站 200。**现在是四份,第五条只会变成五份。**

⚠️ **若某一处拿不到 entry、调不了 `productHref` —— 那本身就是发现,报总工,别绕过去。**

**验收(② 没归零,④ 就没完):**
```
② 仍在产的页面上旧址内链 = 0        ← 现在 283
③ 每个新 href 命中一个真实存在的文件
④ 🔴 负对照：喂一个故意写错的 href，②③ 必须报红
（① 组件元素总数改前后不变 · ⑤ 连跑两次 rebuild 零变化 —— chip 那轮已过，新一轮要重跑）
```

### 第二件:把那个"必须归零的数"做成常驻闸

```
产出里任何 href 命中【旧址形状】即红
   旧址形状 = /{已知机型或品类 slug}/ 或 /{同上}/{数字}
   slug 集合与 switchChipHrefs 同源（entries 的 category 集合），🔴 不另立名单
```
⚠️ **⑥ 之后这类 href 就是死链,而死链不会自己报错。第五套组件哪天冒出来,只有这道闸拦得住。**
⇒ 与残留尺子同规矩:**先喂一个旧址 href 证明它会红,再信它的绿。**

---

## 1b. 已完成(本节仅备查,别重做)

**目标**:`<a class="product-chip" href="/mini/">` 这类 chip,href 与标签**都**从源派生,不再是烘死在 HTML 里的历史值。

**为什么 href 和标签必须一起做**(总工定的,理由不是省事):
```
现在  href 与标签【都】烘死  ⇒ 内部一致（都旧）
只改 href ⇒ 链接指新址、标签说旧名 ⇒ 【新造】一处不一致
```

**⚠️ chip 有两种载体,规则要分开写:**
```
/products/ 上      <button data-filter="mini">Mini …   就地筛选，【没有 href】
机型/品类列表页上   <a class="product-chip" href="/mini/">  导航，有 href
```
仍在产页面上剩的 889 处旧址内链,**全在 `<a>` 那一种上**。

**⚠️ 幂等**:href 改成 `/products/mini/` 之后,下一轮 regen 仍要能认出 slug ——
提取规则必须**同时接受** `/{slug}/` 与 `/products/{slug}/`(含 `/es|pt|zh` 前缀)。一次性正则不够。

**🔴 标签的源有一半到不了 —— 这是 ④b 唯一的跨仓项:**
```
品类 chip（Mounts/Power/Cables/Networking/Cases）
   → FORM_LABEL_KEY + catalog，regenListPage 里已经拿得到  ✅
机型 chip（Mini/Standard/Performance…）
   → locales.json 的 model_display，regenListPage 【拿不到】 🔴
     加参数 = 改 vendored 函数签名，而 Admin 的 src/publish.ts 也 import 并调用它
     ⇒ 必须与 Admin 同批。**不许加"缺了就沿用旧标签"的回落** ——
       它把"忘了传"和"故意不传"变得无法区分，而那正是我们在修的病。
```
⇒ 上一轮停在这里等总工定 A(先做能闭环的两件)还是 B(整批等 Admin)。**开工前先确认这条已定。**

**验收(四条,第 4 条是总工加的):**
```
① 改前后 product-chip 的 <a> 总数不变      （防匹配宽了漏改/多改）
② 仍在产的页面上旧址内链 = 0
③ 每个被改的 href 命中一个【真实存在的文件】—— 形状对不算
④ 🔴 先喂一个【故意写错的 href】进去，判据必须报红
```
> **没有 ④,前三条全绿可能只是因为它们根本没跑到。** 判据自己也要有正对照。

⚠️ 别赶。总工原话:Admin 仍在双写旧详情页(`publish.ts:384`),Joe 编辑产品时旧址页照样更新,**真正冻结的只是"靠全量 rebuild 才生效"的改动,而现在没有这类在排队** ⇒ 停一轮的代价可控。

---

## 2. 验收判据的现状(改过,别用旧的)

**④ 的验收是「仍在产的页面 0 处」,不是「全站 0」。**
```
旧址内链（只数 href；canonical/og:url/hreflang 到 ⑥ 之前继续指旧址，不算违规）
   开工前  8340
   现在    2029 = 889（仍在产，④b 要清零） + 1120（③c-1 冻结的旧详情页，改不动，⑥ 一起消失）
```
> **判据的有效性依赖系统当时的状态。③c-1 冻结了旧详情页,「全站 0」就此结构上不可达。**

---

## 3. 今天在 main 上的链(每条都带自己的证明)

```
07d0d73f1  ③a-1  三处存在性判据 → 新址          产出逐字节零变化
3e13c83be  ③a-2  第五处 urlOf                   零变化；zh 13 行是它抓的
89c47bb71  品类改名换指向（Joe 圈的两处）        611/611 页 header 差异 = 声明的那一处
fede41d6d  og/twitter 与 title 同源              全站 og≠title 42 → 1
7d132af34  ③c-1  停产旧详情页（零删除）          零变化 · 旧址页 190 个仍在
7a5ca8872  redirect-residue 尺子进仓 + 登记       三档 0/1/2 全实测
985d0ba73  ③c-2  渲染源翻转到新址                零变化 · 删旧址后 190/37/312 全守住
ff2022b7f  URL 反斜杠闸（27 道）                  基线 0 · 正对照 2 · 假阳性 0
9a8bc985d  ④a 三处产品链接 + footer 七条          8340 → 2029
eabb0f6a8  ④  首页品类卡 + badge                 909 → 889
```

---

## 4. 🔴 三个陷阱 + 一条方法论(下次开工前必读)

**① 本地 `main` 分支陈旧** —— `git branch --contains` 会因此说假话。判真线只看 `git ls-remote origin refs/heads/main`。

**② 5a(`6ef06db6a`)仍压着**,含 301 表(231 条)+ 29 挂载桩 + `legacy-redirect.js`。
🔴 **冲突取【并集】,别取一边** —— 5a 写于 ⑩ 号闸落 main 之前,取它那边会**静默删掉 ⑩**。`scripts/i18n-check.mjs` 尤其。

**③ main 常驻两道红**,来自 Admin 直写(不过闸):`es-hold-check` 4 处 · `pt-leak-scan` 90。**推送时必须与基线逐字相同,不许变大。**

**④ ⑥ 的"停写旧列表页"是【两处】,漏一处的后果最毒:**
```
阶段② hOld 写入      ← 明显
阶段① 播种（缺页就建）← 容易漏：它把刚删的页当"缺页"重新播种出来
```
实测:删 227 后 rebuild,旧详情页 **0/190**(不回来)、旧列表页 **37/37**(**全被重建**)。
漏了它 ⇒ 删完当时对,下一轮 rebuild 悄悄全回来,而那时 noindex 已关、sitemap 已换 ⇒ **新旧同时可收录,全站 200,没人会注意到。**
⇒ 验收挂在尺子上:**删完 + 再 rebuild 一轮之后,尺子仍报 exit 0。**
> **一次删除的验收,不能只在删除那一刻做。**

---

## 5. 今天学到的、值得带走的四条

**① 零变化验收必须先证明改动还在。**
我跑 `git checkout -- .` 想清理失败运行的产出,**它把我自己的改动一起 revert 了**,接着 rebuild 得到"0 文件 / 增 0 / 删 0",差点当验收通过。
> **"改动很干净"的零变化,和"改动已经不在了"的零变化,长得一模一样。**
⇒ 固定两步:**① `grep -c <新符号>` + `git diff | wc -l` 证明改动在 ② 再读那个 0。**
⚠️ 而 `git checkout -- .` / `reset --hard` / `clean -fd` 在共享树上一律先报总调度。**"清理一下环境"是最容易越过安全边界的动机,因为它感觉不像在改东西。**

**② 用不变量代替枚举。**
总工给的四处存在性判据,实际是六处。三处迁完**逐字节零变化**之后我没停,在复刻树上真删一次旧址 —— 详情页从 68 救回 190,**而 alternate 仍是 190 不是 312** ⇒ 揪出第五处 `urlOf`。
> **「零变化」只证明没改坏,不证明改够了。**
> **一份必须完整的清单,和一个必须成立的不变量 —— 后者永远更强,它不依赖任何人数得全。**

**③ 存在性判据锚在会消失的地址上 —— 今天出现三次。**
`regen` 的 249/292/490 · 第五处 `urlOf` · 首页品类卡旁边的 `{{badge.X}}`。
共同形状:**旧址一删,它们不报错,而是【改变主意】** —— 认为那些页本来就不该有,于是静默停产/静默少发。**全站 200,没有一个 404 会提醒任何人。**

**④ 三档退出要全测,不是只测好测的那两档。**
尺子移植时我引入了一个 bug:`fs.existsSync(裸路径)` 对**目录**为 true,删干净后仍报剩余 37 ⇒ **`exit 0` 那一档永远到不了,而尺子看起来一直正常**。原版走 `git ls-tree`(树里没有目录条目),**同一行代码换到文件系统就变了意思**。
> **「没先见过非零的 0 不作数」反过来也成立 —— 没见过 0 的尺子,你根本不知道它的 0 到不到得了。**

**⑤ 结论活得比它的条件久。** 今天这个形状在总工和我两边各出现了两次(「不许调主次」的作用域是 ③a、「全站 0」定于 ③c-1 之前)。
⇒ 给约束时带作用域,改自己的指令时说明白是「补充」还是「取代」。**提炼本身就是剥离条件的动作。**

---

## 6. 未推

**没有。** `origin/main` 与本地 `gate9` 相同,工作树 0 项。
