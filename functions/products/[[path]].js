/**
 * /products/* 的挂载点(en)。判据本身在 functions/_lib/product-route.js —— 四个挂载点共用。
 *
 * 🔴 第一条、也是最容易致命的一条:**CF Pages Functions 优先于静态资源。**
 *    这个文件挂在 `/products/[[path]]` 上,于是【所有】/products/* 请求都先进这里 ——
 *    包括本该由静态文件服务的分类页与产品详情页。
 *    **漏掉 `context.next()` 就是产品区全站白屏**,而闸不会发现:闸看的是产出文件,
 *    文件确实都在;变的是"谁来响应"。同目录的 functions/scripts/[[path]].js 正是靠同一个
 *    机制把整个前缀强制 404 —— 同一把刀,两个方向。
 *
 * 🔴 第二条,是它的镜像,而且在产上真的坏了两天:**挂载点只覆盖自己那一条路径。**
 *    这个文件【不】接管 /es/products/*。语种前缀要各自的挂载文件 —— 就是隔壁那三个。
 *    删掉其中任何一个,那个语种的归一化就静默失效(返回 404 而不是 301),
 *    而所有闸依旧全绿。详见 _lib/product-route.js 顶部。
 *
 * ⚠️ 这一层【不做旧址 → 新址的 301】。我最初的五步计划把它排在这里并写着"旧址仍有静态页兜底",
 *    那句是错的:**Function 一旦返回 301,静态页就不会被服务了,双活当场结束。**
 *    旧址 301 与"删旧址静态页 + 切内链 + 换 sitemap"是同一件事,归第 5 步。
 */
export { handleProductRoute as onRequest } from "../_lib/product-route.js";
