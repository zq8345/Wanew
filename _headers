# Cloudflare Pages 响应头规则（#66 图片提速）
#
# 在此之前【没有这个文件】= 所有静态资源吃 Pages 默认缓存，回访者每次都要重新验证。
# 图片是站上最大的字节来源（单张最大 3.3MB），缓存规则是这里性价比最高的一刀：
# 改一个文件，0 行页面改动，回访直接从磁盘取。
#
# ⚠️ 只对【内容不变或带版本号】的路径用 immutable：
#   /static/upload/*  上传资产按时间戳/哈希命名，改图＝换文件名，不会原地变 → 安全
#   /skin/*           CSS 走【内容指纹文件名】w3.<hash>.css（scripts/css-hash.mjs 生成），改内容＝换文件名 → 安全
#   HTML              绝不长缓存：内容会原地更新，长缓存会把旧页面钉死在用户机器上
#
# 顺序：Pages 取【最先匹配】的规则，所以更具体的写在前面。

/static/upload/*
  Cache-Control: public, max-age=31536000, immutable

/static/*
  Cache-Control: public, max-age=31536000, immutable

# ⚠️ 实测留档 —— 上一条(2026-07-27)说"本条当前不生效、被 zone 规则盖成
#    max-age=14400, must-revalidate"，**2026-07-28 复测已不成立**：
#    /skin/css/w3.css?v=65、无参、随机参三种取法【全部】返回 max-age=31536000, immutable。
#    (对照:HTML 侧正确地是 max-age=0, must-revalidate。)
#    留下这段是因为**一条与现实不符的注释比没有注释更会骗人** —— 它当时救过我们一次判断，
#    现在它的结论反了，就必须原地更正而不是删掉,否则下次有人照着旧结论做决定。
#
# 🔴 immutable 一年真的生效 = 一旦某个 URL 上存进了错的字节，它不会自愈。
#    这正是 2026-07-28 `?v=60` 事故的成因：部署未传开时有请求打了新版本号，
#    边缘回源拿到旧文件并钉一年，purge 也清不掉已进访客浏览器的那份。
#    → CSS 改走【内容指纹文件名】(scripts/css-hash.mjs)：部署窗口里的请求打到
#      一个尚不存在的路径 = 404(短暂、自愈、看得见)，而不是拿到旧字节钉一年。
#    旧的 /skin/css/w3.css?v=* 【不做 301】：让它自然烂掉即可(_redirects 额度也只剩 6 条)。
/skin/*
  Cache-Control: public, max-age=31536000, immutable

/favicon.ico
  Cache-Control: public, max-age=604800

/favicon.svg
  Cache-Control: public, max-age=604800

# HTML：允许 CDN 短缓存 + 必须回源校验，保证内容更新能立刻见效
/*.html
  Cache-Control: public, max-age=0, must-revalidate

/
  Cache-Control: public, max-age=0, must-revalidate
